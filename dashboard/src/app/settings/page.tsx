import { DashboardShell } from "@/components/dashboard-shell"; import { PageHeader } from "@/components/page-header";
export default function SettingsPage() { return <DashboardShell><PageHeader title="Console settings" description="Session and display preferences for this browser."/><section className="surface max-w-2xl rounded-2xl p-6"><h2 className="font-semibold">Session security</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">The current dashboard session is stored in an HTTP-only cookie and expires after 12 hours. Use the Sign out action to revoke it in this browser.</p><div className="mt-5 rounded-xl border border-[var(--border)] bg-black/10 p-4"><p className="text-sm font-medium">Theme</p><p className="mt-1 text-sm text-[var(--muted)]">The Shield console uses its hardened dark visual theme.</p></div></section></DashboardShell>; }
