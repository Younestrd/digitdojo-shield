import { CircleHelp } from "lucide-react";
export function CapabilityNotice({ capability, detail }: { capability: string; detail: string }) { return <section className="surface rounded-2xl p-6"><div className="flex gap-3"><CircleHelp className="mt-0.5 shrink-0 text-[var(--accent)]" size={20}/><div><h2 className="font-semibold">{capability} is not exposed by this Shield daemon</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">{detail} The console intentionally does not generate simulated records. This screen will activate when the daemon provides this capability through its authenticated REST API.</p></div></div></section>; }
